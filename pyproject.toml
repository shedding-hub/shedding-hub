[project]
name = "shedding-hub"
version = "0.1.3"
readme = "README.md"
description = "Data and statistical models for biomarker shedding."
dependencies = [
    "pyaml",
    "requests",
    "pandas",
    "matplotlib",
    "numpy",
    "scipy"
]

[tool.setuptools.packages]
find = {}
