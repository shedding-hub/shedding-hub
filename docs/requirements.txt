# Read the Docs builds from this file. Pinned to the same versions the CI
# lockfile (../requirements.txt) resolves, so the published site and the
# `mkdocs build --strict` gate in CI cannot diverge: unpinned, CI would stay
# green on its pins while the published build floated onto a new release.
# Material's own build banner warns that MkDocs 2.0 will be backward
# incompatible and break every plugin, so this is a live risk, not a
# hypothetical one.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings[python]==1.0.6
mkdocstrings-python==2.0.7
-e .
